class averageQueue:

    def __init__(self,maxSize=20):
        self.__maxSize=20
        self.__queue=list()

    def add(a):
        """enqueues an item; if the items in the queue equal or exceed the maxSize then  the oldest is removed.
        if a None is passed one item is removed."""
        pass

    def get_avg(self):
        """:returns average of all values without removing any from the queue"""
        pass